<nav class="navbar navbar-expand-lg bg-light sticky-top border-bottom border-secondary">
	<div class="container-fluid">
		
		<a class="navbar-brand" href="{{ path('app_main') }}" style="color: #2c3e50">
			<img src="{{ asset('assets/local-img/logo.png') }}" width="50px" alt="logo"> <b>Checking System</b>
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				{# <li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{ path('app_main') }}">Home</a>
				</li> #}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_calendar_index') }}">All Calendar</a>
				</li>
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						Students
					</a>
					<ul class="dropdown-menu">
						<li>
							<a class="dropdown-item" href="{{ path('app_student_presence') }}">Presence/Absense</a>
						</li>
						<li><hr class="dropdown-divider"></li>
						<li>
							<a class="dropdown-item" href="{{ path('app_student_all') }}">All Students</a>
						</li>
					</ul>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_users') }}">Users</a>
				</li>
				{% if is_granted('ROLE_ADMIN') %}
					<li class="nav-item">
						<a class="nav-link disabled">Admin</a>
					</li>
				{% elseif is_granted('ROLE_USER') %}
					<li class="nav-item">
						<a class="nav-link disabled"></a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link disabled"></a>
					</li>
					{% endif %}

				</ul>
				{% if app.user %}
					<ul class="navbar-nav ml-auto">
						<li class="nav-item">
							<a class="nav-link" href="{{ path('app_profile_index') }}">{{ app.user.firstName }}
								{{ app.user.lastName }}</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{ path('app_logout') }}"> <img src="{{ asset('assets/local-img/button/logout.png') }}" width="20px" alt="logout"></a>
						</li>
          {% if is_granted('ROLE_ADMIN') %}
	
        {% endif %}

				{% else %}
					<ul class="navbar-nav ml-auto">
						<li class="nav-item">
							<a class="nav-link" href="{{ path('app_login') }}">Login</a>
						</li>
					</ul>
				{% endif %}
			</div>
		</div>
	</nav>
		     {# {% if is_granted('ROLE_ADMIN') %}
						<div class="text-center mt-2">
                        <a href="{{ path('app_register') }}" class="btn btn-primary">Add an user</a>
                        </div>
            {% endif %} #}
